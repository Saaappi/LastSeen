-- Namespace Variables
local addon, addonTbl = ...;

-- Module-Local Variables
local L = addonTbl.L;
local MODE_DROPDOWN = {
	L["DEBUG_MODE"],
	L["NORMAL_MODE"],
	L["QUIET_MODE"],
}

addonTbl.MODE_DROPDOWN = MODE_DROPDOWN;